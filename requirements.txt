# Physics-Informed DeepONet
#
# Recommended runtime used by this project:
#   Python 3.10
#   CUDA 12.x
#
# Install PyTorch with the CUDA build that matches your machine first.
# Example:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

# Core training / evaluation
torch>=2.0
numpy>=2.0
scipy>=1.13
matplotlib>=3.9
h5py>=3.8
tqdm>=4.60
lpips>=0.1.4

# Data generation scripts in data_generation/ use scipy sparse solvers,
# scipy.io, scipy.ndimage, numpy, and matplotlib; they are covered above.

# Optional legacy script:
#   PML_qSNN_VQ_freesurface.py depends on TensorFlow v1-style APIs.
#   Keep it out of the default environment unless that script is needed.
# tensorflow>=2.10
